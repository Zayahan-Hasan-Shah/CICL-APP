import 'package:flutter/material.dart';

class AppColors {
  static const brightYellowColor = Color(0xFFFFD600);
  static const whiteColor = Colors.white;
  static const purpleColor = Color(0xFF7B2FF2);
  static const orangeColor = Color(0xFFE96443);
  static Color greenColor = Colors.green.shade600;
  static Color redColor = Colors.red.shade600;
  static Color greyColor = Colors.grey.shade600;
  static Color lightGreyColor = Colors.grey;

  static const backgroundGradientColor = [
    brightYellowColor,
    whiteColor,
    whiteColor
  ];

  static const borderGradientColor = [brightYellowColor, purpleColor];

  // =======================================================================
  static const Color backgroundColor = Color.fromRGBO(4, 96, 158, 1);
  static const buttonColor1 = Color(0xFF006CB5);
  static const buttonColor2 = Color(0xFF143E4E);
  static const inputBackgroundColor = Color(0xFFF0F0F0);
  static const restrictionColor = Color(0xFF808080);
  static const bgFamDetailColor = Color(0xFFEAEAEA);

  static const buttonGradientColor = [buttonColor1, buttonColor2];
}
